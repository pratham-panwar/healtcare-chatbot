# scikit-learn is pinned to the exact version the model was trained with.
# .joblib files are pickles and can break (or silently misbehave) on other versions.
scikit-learn==1.9.1
numpy>=1.26
pandas>=2.0
joblib>=1.3
rapidfuzz>=3.0
